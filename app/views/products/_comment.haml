%article{class: 'media', id: "comment-#{comment.id}"}
  .media-left
    %strong= comment.user.username
  .media-content
    .content
      %p= comment.body
  .media-right
    = link_to 'x', comment_path(comment), method: "delete", remote: true, data: { confirm: 'Etes vous sûr de vouloir supprimer ce commentaire?' }